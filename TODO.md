# Task: Fix logo.svg not perfect

- [ ] Inspect how logo is rendered in `index.html` and how it should fit inside `.logo-box`
- [ ] Update SVG rendering attributes (e.g., preserveAspectRatio) or switch to using `images/logo.svg` directly
- [ ] Ensure the logo scales cleanly in all mobile sizes (no cropping/overflow)
- [ ] Validate that `index.html` has no syntax issues

